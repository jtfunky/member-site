# Secrets — NEVER commit (DB password + API keys live here)
includes/config.php
includes/config.local.php

# User uploads (proof / audio / avatar files) — user data, not code
uploads/audio/*
uploads/avatars/*
uploads/proofs/*
uploads/investor-proofs/*
!uploads/audio/.htaccess
!uploads/avatars/.htaccess
!uploads/proofs/.htaccess
!uploads/investor-proofs/.htaccess

# Diagnostics / temp probes
_opcache-check.php
_diag.php
_min.php
signuptest.php
*.log

# OS / editor junk
.DS_Store
Thumbs.db
*.swp
*.tmp

# Downloaded tools / large binaries (not project code)
*.exe
loadtest/k6.exe
